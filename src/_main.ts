
import {
    loadAsyl, loadFromBytes, loadFromDataUri, loadFromUrl,
    memory, dataView, asyl,
}
from './loader';

globalThis.asyl =
{
    loadAsyl,
    loadFromBytes,
    loadFromDataUri,
    loadFromUrl,

    memory,
    dataView,
    asyl,
};

